var expect = require('chai').use(require('chai-as-promised')).expect;

describe('Signer', function () {
  describe('create a signer', function () {
    it('should throw error if a property is missing', function () {
      // TODO
    });
  });
});
